# Plan: Commit and Push All Changes

## Current State
- Branch: `main`, up to date with `origin/main`
- Remote: `https://github.com/Piercekaoru/aijinapi.git`
- No staged changes — all changes are unstaged or untracked

## Changes to Commit

### Unstaged (modified) — 24 files
Includes updates across:
- **Frontend**: Dockerfile, HTML pages (landing/login/models), layout, components (SiteHeader, SiteFooter), client pages (account, dashboard, login, playground, docs), html-page.ts, site-routes.ts
- **Backend**: .env.example, migrate.rs, config.rs, db.rs, routes.rs, upstream.rs
- **Tests**: http_proxy.rs, subscription_quota.rs, upstream_proxy.rs
- **Docker**: docker-compose.yml

### Untracked (new) — 8 items
- Japanese locale HTML: `aijinapi-landing-ja.html`, `aijinapi-login-ja.html`, `aijinapi-models-ja.html`
- Locale routing: `app/[locale]/` directory
- Terms page: `app/terms/`
- Locale layout: `components/LocaleClientLayout.tsx`
- i18n library: `lib/i18n/`
- Middleware: `middleware.ts`

## Steps

1. **Stage all changes**: `git add -A`
2. **Commit**: Create a descriptive commit message reflecting the i18n/localization feature + other updates
3. **Push**: `git push origin main`

## ⚠️ Security Check
Before committing, verify no secrets are staged (`.env` files, API keys). The `.env.example` is safe to commit; real `.env` files should be in `.gitignore`.
