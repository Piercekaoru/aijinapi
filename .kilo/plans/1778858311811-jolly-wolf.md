# Plan: Clean Up Duplicate Photos

## Root JPG Status

Root has **23 JPG files**. 14 of them already exist in `public/images/` (duplicated on disk). 5 of the remaining 9 are never referenced in any code at all.

## Docs Page Model Cards — Image Reuse

`app/docs/page.tsx` has 15 model cards but only 10 unique images. **5 images are used twice:**

| Duplicate Image | Model A (keep) | Model B (needs new) | Line to change |
|---|---|---|---|
| `GrHDjXQXYAACVsc.jpg` | big-pickle | nemotron-3-super-free | :91 |
| `HHZ_hQzbIAEw83V.jpg` | deepseek-v4-flash-free | deepseek-v4-flash | :139 |
| `HAh3SWLacAAA6By.jpg` | minimax-m2.5-free | mimo-v2.5 | :147 |
| `HG42ZYwa8AAGBEd.jpg` | ring-2.6-1t-free | kimi-k2.5 | :115 |
| `Gth0fxcawAARQmB.jpg` | glm-5.1 | qwen3.5-plus | :171 |

## How Many Photos Needed

**5 new unique JPGs** — one per duplicate model card listed above. Place them in `public/images/`.

## Steps

1. Delete the 9 junk/unused JPGs from project root (5 never-referenced + 4 duplicated-to-public/images but never used in code)
2. Delete the 14 root JPGs that already have copies in `public/images/`
3. Source 5 new JPG images
4. Copy them to `public/images/`
5. Update the 5 `image` lines in `app/docs/page.tsx` model array with new filenames
